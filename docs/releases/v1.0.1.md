# v1.0.1 — Release Instructions Consistency Fix

本补丁版本修正 v1.0.0 测试指南中的仓库入口：clone 地址统一为
`1667001332/guided-story-video-agent`，并默认检出 `v1.0.1` 标签。

代码与 Provider Failure Protocol 没有功能变化。本版本只修正交付文档，避免同学按照
测试指南进入旧的 `guided-story-video-agent-v2` 仓库。

安装与验收仍需遵守 v1.0.0 的边界：离线 Fake/Mock 测试不等同于真实文本模型、真实视频
Provider 或付费接口验收；真实 API 必须使用测试者自己的密钥。
